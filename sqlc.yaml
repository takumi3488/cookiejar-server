version: "2"
sql:
  - schema: "schema.sql"
    queries: "queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_interface: false
        emit_exact_table_names: false
